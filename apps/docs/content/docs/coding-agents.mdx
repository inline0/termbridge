---
title: Coding agents
description: Install and authenticate Claude, Codex, and OpenCode in Daytona sandboxes.
---

# Coding agents

Termbridge can prepare coding agents inside Daytona sandboxes by installing their CLIs and syncing your existing auth. This keeps the sandbox ready to use without re-logging in.

## Supported agents

- **Claude Code** (`claude`)
- **Codex** (`codex`)
- **OpenCode** (`opencode`)

## Automatic setup (recommended)

Enable automatic setup by selecting agents explicitly:

```bash
TERMBRIDGE_DAYTONA_AGENTS=claude,codex,opencode
```

Or enable all known agents:

```bash
TERMBRIDGE_DAYTONA_AGENT_AUTO=true
```

When automatic setup is enabled, Termbridge will:

- Install agent CLIs inside the sandbox (via `npm install -g`).
- Sync local auth files into the sandbox.
- Forward supported API keys into the tmux session.

Local auth files are discovered from these defaults:

- `~/.claude.json`
- `~/.codex/auth.json`
- `~/.config/opencode/opencode.json`

## Manual auth sync

If you prefer manual control, you can specify auth paths directly:

```bash
TERMBRIDGE_DAYTONA_AGENT_AUTH_PATHS=~/.claude.json,~/.codex/auth.json,~/.config/opencode/opencode.json
```

Need custom destinations inside the sandbox? Use explicit mappings:

```bash
TERMBRIDGE_DAYTONA_AGENT_AUTH_MAPS=~/.claude.json=/home/daytona/.config/claude/auth.json
```

## API keys and extra env vars

Termbridge forwards common agent API keys automatically. Add additional env var names via:

```bash
TERMBRIDGE_DAYTONA_AGENT_ENV=CUSTOM_TOKEN,ANOTHER_SECRET
```

## Controlling installation

If you want to skip installing CLIs in the sandbox, set:

```bash
TERMBRIDGE_DAYTONA_AGENT_INSTALL=false
```

To override the install packages entirely:

```bash
TERMBRIDGE_DAYTONA_AGENT_PACKAGES=@anthropic-ai/claude-code,@openai/codex,opencode
```

<Callout type="info">
  Auth sync copies local credentials into the sandbox. Only use this with sandboxes you trust, and prefer ephemeral sandboxes with `TERMBRIDGE_DAYTONA_DELETE_ON_EXIT=true`.
</Callout>

## Related docs

- [Sandboxes](/docs/sandboxes)
- [CLI](/docs/cli)
